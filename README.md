# XmR Tool

A standalone Streamlit app for Individuals & Moving Range (XmR) charts.

## Run
pip install -r requirements.txt
streamlit run app/app.py

## Core logic
Pure Python XmR functions live in xmr_core/xmr.py.
